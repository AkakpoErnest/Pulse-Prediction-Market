{
  "name": "pulse-market",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "compile": "npm run compile --workspace=contracts",
    "test": "npm run test --workspace=contracts",
    "deploy:testnet": "npm run deploy:testnet --workspace=contracts",
    "clean": "npm run clean --workspace=contracts && npm run clean --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
